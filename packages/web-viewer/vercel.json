{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && (cd vendor-src && pnpm install --frozen-lockfile --ignore-workspace) && pnpm prebuild:vendor && pnpm --filter @stele/web-viewer build",
  "installCommand": "echo 'install handled inside buildCommand'",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/((?!.*\\.).*)", "destination": "/index.html" }
  ]
}
